import React, { useState } from 'react';

const INITIAL_CASES = [
	{ id: 'listing-1', type: 'Listing verification', subject: '4-bedroom duplex in Chevron', owner: 'Prime Heritage Realty', detail: 'Ownership documents awaiting review', priority: 'High' },
	{ id: 'agent-1', type: 'Agent verification', subject: 'Agent accreditation renewal', owner: 'Adeola Property Partners', detail: 'License document submitted', priority: 'Normal' },
	{ id: 'fraud-1', type: 'Fraud report', subject: 'Possible inspection fee request', owner: 'Unverified contact', detail: 'Two renter reports received', priority: 'Urgent' },
	{ id: 'dispute-1', type: 'Dispute', subject: 'Deposit refund disagreement', owner: 'Renter and landlord', detail: 'Evidence uploaded for review', priority: 'High' },
];

export default function AdminPage({ onNavigate = () => {} }) {
	const [cases, setCases] = useState(INITIAL_CASES);
	const [notice, setNotice] = useState('');
	const resolve = (id, action) => { setCases((items) => items.filter((item) => item.id !== id)); setNotice(`${action} recorded and the audit trail was updated.`); };
	return <main className="min-h-screen bg-gray-50 px-4 py-10 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-indigo-700">Admin / Operations</p><h1 className="mt-2 text-3xl font-black text-gray-900">Trust and moderation queue</h1><p className="mt-2 text-sm text-gray-500">Verify listings and agents, moderate content, handle fraud reports, and resolve disputes.</p></div><button type="button" onClick={() => onNavigate('home')} className="text-xs font-bold text-gray-600 underline">Back to marketplace</button></div>{notice && <div role="status" className="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-xs font-bold text-emerald-800">{notice}</div>}<div className="mt-8 grid gap-4 sm:grid-cols-4"><div className="rounded-2xl border border-gray-200 bg-white p-5"><p className="text-xs font-bold uppercase text-gray-500">Open cases</p><p className="mt-2 text-3xl font-black">{cases.length}</p></div><div className="rounded-2xl border border-gray-200 bg-white p-5"><p className="text-xs font-bold uppercase text-gray-500">Listing checks</p><p className="mt-2 text-3xl font-black text-emerald-700">1</p></div><div className="rounded-2xl border border-gray-200 bg-white p-5"><p className="text-xs font-bold uppercase text-gray-500">Fraud reports</p><p className="mt-2 text-3xl font-black text-rose-700">1</p></div><div className="rounded-2xl border border-gray-200 bg-white p-5"><p className="text-xs font-bold uppercase text-gray-500">Disputes</p><p className="mt-2 text-3xl font-black text-indigo-700">1</p></div></div><section className="mt-8 space-y-3">{cases.map((item) => <article key={item.id} className="flex flex-col gap-4 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm md:flex-row md:items-center md:justify-between"><div><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-gray-100 px-2 py-1 text-[10px] font-black uppercase text-gray-700">{item.type}</span><span className={`rounded-full px-2 py-1 text-[10px] font-black uppercase ${item.priority === 'Urgent' ? 'bg-rose-100 text-rose-800' : item.priority === 'High' ? 'bg-amber-100 text-amber-800' : 'bg-blue-100 text-blue-800'}`}>{item.priority}</span></div><h2 className="mt-2 text-base font-black text-gray-900">{item.subject}</h2><p className="mt-1 text-xs text-gray-500">{item.owner} · {item.detail}</p></div><div className="flex gap-2"><button type="button" onClick={() => resolve(item.id, item.type === 'Fraud report' ? 'Fraud case escalated' : 'Case approved')} className="rounded-xl bg-emerald-600 px-4 py-2 text-xs font-black text-white">Review and resolve</button><button type="button" onClick={() => resolve(item.id, 'Case escalated')} className="rounded-xl border border-gray-300 px-4 py-2 text-xs font-black text-gray-700">Escalate</button></div></article>)}{!cases.length && <div className="rounded-2xl border border-dashed border-gray-300 bg-white p-12 text-center text-sm text-gray-500">All verification, fraud, and dispute cases are up to date.</div>}</section></div></main>;
}
